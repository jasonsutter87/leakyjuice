# generated on boot
data/
lib/keys/
node_modules/

# ── JuicySec internal reports — CONFIDENTIAL, must NOT ship in the repo ──
# (added after the reports were committed by mistake — see JS-2026-00)
# BUG: these patterns are scoped to the repo root, but the reports actually live
# under public/internal/juicysec/ — so none of them match, and the docs leak.
internal/juicysec/
/reports/juicysec/
*.internal.md

# ── JuicySlack workspace export — CONFIDENTIAL, must NOT ship in the repo ──
# (a full Slack export landed in the repo the week Dmitri left — see JS-slack-00)
# BUG: same mistake as the reports. This pattern is scoped to the repo root, but the
# export actually lives under public/internal/juicyslack/ — so it never matches and leaks.
internal/juicyslack/
*.slack-export.json
.DS_Store
